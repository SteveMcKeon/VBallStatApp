#New Features
add "export highlights" (for player / action type) functionality, ffmpeg
Add functionailty for users to add stats so I don't have to do it all myself (this will require automating the rally/seq/touch/score, perhaps even timestamp numbering).
Add video player settings option to change settings such as "jump forward/backward" timings, delay after end of rally before skipping to next rally during autoplay
Add video upload functionaility
	-Automate encoding pipeline.
Add tooltip intro for new users to introduce features.
Add resize video option (drag) in side-by-side mode.
	Shift + -> and <- for seek to set.
	Shift + n for next video (by date).
Resize table columns button?
Add contact / bug report / suggestions email?
Register team, invite other users to team.
Add comments instead of notes.
team tactics or certain rotation options - by each team captain (Ask Tamir)

#Fixes
REFACTOR

git add .

git commit -m "feat: Implement video upload workflow with processed game filtering and hot-reload dev setup
- Added drag-and-drop video upload modal with MP4 validation and dynamic filename generation
- Implemented server-side upload handling with Multer, saving files to user-uploads directory
- Automatically generates game title and filename based on upload date and existing game count
- Inserted uploaded games into Supabase with team name, players, and processing flags
- Filtered unprocessed games from GameSelector and StatsSummary dropdowns
- Ensured UploadGameModal closes video player controls overlay programmatically
- Added frontend form validations for file type, required fields, and UI consistency (fixed dropzone height)
- Integrated Express + Vite hot-reload dev workflow in Docker"

git push


Conrad, Liz, Natacha, Tamir, Taylor, Yuzhou